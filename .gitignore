# Credentials — never commit, this repo is public
.secrets/

# Build artifacts — regenerate from wp-plugin/gemz-affiliate-tracker/ source instead
wp-plugin/*.zip

# Scratch/debug output from local testing
*.log
